import Button from '../src/Button'

# Button
Core button component

## Usage Guidelines
- Can be used in groups
- Can be used with pre or suff icons
- Comes in a limited set of variants

<div>
  <Button>Default</Button>
  <Button ml={2} variant="primary">Primary</Button>
  <Button ml={2} variant="destructive">Destructive</Button>
  <Button ml={2} variant="link">Link</Button>
</div>
<div>
  <br/>
  <Button size='small'>Small</Button>
  <Button ml={2} variant="primary" size="small">Primary</Button>
  <Button ml={2} variant="destructive" size="small">Destructive</Button>
  <Button ml={2} variant="link" size="small">Link</Button>
</div>


```
<Button>Default</Button>
<Button variant="primary">Primary</Button>
<Button variant="destructive">Destructive</Button>
<Button variant="link">Link</Button>

<Button size='small'>Small</Button>
<Button variant="primary" size="small">Primary</Button>
<Button variant="destructive" size="small">Destructive</Button>
<Button variant="link" size="small">Link</Button>
```
